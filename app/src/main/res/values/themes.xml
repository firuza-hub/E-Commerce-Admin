<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ECommerceAdminAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/nunito_regular</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:windowLightStatusBar" >true</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

        <item name="card_border_color">@color/input_card_border_light</item>
        <item name="card_background_color">@color/input_card_background_light</item>
        <item name="text_color">@color/black</item>
        <item name="hint_text_color">@color/input_hint_color_light</item>
        <item name="input_hint_icon_color">@color/input_hint_color_light</item>
        <item name="btn_inactive_color">@color/input_hint_color_light</item>
        <item name="btn_active_color">@color/black</item>
        <item name="btn_text_color">@color/white</item>
        <item name="accent_color">@color/accent_carrot</item>
    </style>
</resources>